/**
 * Configuración de Sentry para monitoreo de errores y performance
 * 
 * Se inicializa automáticamente cuando se importa.
 * En desarrollo, los errores solo se muestran en consola.
 */

// Este archivo se inicializará desde el cliente usando @sentry/nextjs
// La configuración real está en sentry.client.config.ts y sentry.server.config.ts

export {};
